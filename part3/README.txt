To run in command line:
Navigate to the directory that contains the .jar file. Run using java -jar perceptron.jar [args]
The program takes one argument, that being the name of the data file (e.g. ionosphere.data).
 It outputs the final accuracy, number of repeats, and number of instances still incorrect.